services:
  - type: web
    name: scan-edit-pro-ai
    env: docker
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHONUNBUFFERED
        value: "1"
    # Removed the disk section because it's not allowed on 'free' plan
